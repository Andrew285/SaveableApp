package com.rainyday.saveableapp.ui.theme

/**
 * Canonical alpha scale for `Color.copy(alpha = ...)` calls — tinted badges, selected-state
 * backgrounds, muted icons, disabled content, etc. Named after their own value for the same
 * reason as [Dimens]: usage context lives at the call site, not in the token name.
 */
object AppAlpha {
    const val a10 = 0.10f
    const val a12 = 0.12f
    const val a14 = 0.14f
    const val a16 = 0.16f
    const val a18 = 0.18f
    const val a38 = 0.38f
    const val a40 = 0.40f
    const val a60 = 0.60f
}
